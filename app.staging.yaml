runtime: nodejs20
service: default
instance_class: F1

env_variables:
  NODE_ENV: staging
  PORT: 8080

automatic_scaling:
  min_instances: 0
  max_instances: 2

handlers:
  - url: /.*
    script: auto
    secure: always
