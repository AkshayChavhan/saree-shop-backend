runtime: nodejs20
service: default
instance_class: F2

env_variables:
  NODE_ENV: production

automatic_scaling:
  min_instances: 1
  max_instances: 10

handlers:
  - url: /.*
    script: auto
    secure: always
